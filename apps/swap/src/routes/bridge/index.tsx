import { createFileRoute } from "@tanstack/react-router";
import { BridgePage } from "components/pages";

export const Route = createFileRoute("/bridge/")({
  component: BridgePage,
});

const nima = () => {};

nima();
