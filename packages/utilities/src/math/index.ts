export const multiply = ({ a, b }: { a: number; b: number }) => {
  return a * b;
};

export const sum = ({ a, b }: { a: number; b: number }) => {
  return a + b + 8;
};

export const divide1 = ({ a, b }: { a: number; b: number }) => {
  return a / b;
};

export const divide2 = ({ a, b }: { a: number; b: number }) => {
  return a / b;
};

export const divide3 = ({ a, b }: { a: number; b: number }) => {
  return a / b;
};

export const divide4 = ({ a, b }: { a: number; b: number }) => {
  return a / b;
};

export const divide5 = ({ a, b }: { a: number; b: number }) => {
  return a / b;
};

// console.error(123);
